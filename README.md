# CI/CD Pipeline with GitHub, Jenkins, and Docker

This repository contains a sample Flask application with tests and a full CI/CD pipeline using:

- GitHub (source control)
- Jenkins (running in Docker)
- Docker (build + deploy)

Pipeline Stages:
1. Clone repository
2. Install dependencies
3. Run tests
4. Build Docker image
5. Run Docker container locally
